query lookupDomainQuery($domain: String!) {
  domainLookup(id: $domain) {
    domain
    status
    create_date
    update_date
    whois_server
    registrar {
      name
      url
    }
    registrant {
      name
      organization
      region
      country
      email
      phone
    }
  }
}